# kp-feature

Фронтенд-модуль **коммерческого предложения (КП)**: конструктор, витрина каталога, шаблон печати.

This library was generated with [Nx](https://nx.dev).

## Конструктор КП (`KpBuilderPage`)

Позиции редактируются **только в превью документа** (таблица на макете A4). Кнопки удаления и поля ввода помечены классом `kp-no-print` и в `@media print` не попадают в PDF; в печати поля отображаются как текст.

Колонка **Фото** показывается только если хотя бы у одной строки заполнен `imageUrl` (витрина или вручную); иначе колонка скрыта, поле остаётся в форме через скрытый input. В таблице сначала строки с непустым URL, затем остальные.

## Витрина каталога (`KpCatalogVitrineComponent`)

По умолчанию показываются **демо-товары** из `kp-catalog-demo-products.ts`. Чтобы подставить данные из своей таблицы или API, передайте массив в инпут **`[catalogProducts]`** (`KpCatalogProduct[]`). Пустой массив — пустая витрина; если инпут не задан, остаются демо-данные.

См. также JSDoc на `@Input() catalogProducts` в `kp-catalog-vitrine.component.ts`.
