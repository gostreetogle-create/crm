/**
 * Public legacy redirects that must stay in parity between `crm-web` and `srm-front`.
 * Keep this list small and explicit to avoid route drift.
 */
export const DICTIONARIES_PUBLIC_REDIRECT_SEGMENTS = ['dictionaries', 'materials', 'geometries'] as const;
