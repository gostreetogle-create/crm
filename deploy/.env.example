# Скопируй в `deploy/.env` и замени значения. Файл `.env` в git не коммитится.

BACKEND_PORT=3000
WEB_PORT=8080

# Если backend и web под одним доменом через nginx (как в этом деплое), можно оставить '*'
# или указать origin (например https://srmweb.com).
CORS_ORIGIN=*

