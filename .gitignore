# Local raw sources (not for git)
Basics/
Исходники/

backend/.env
deploy/.env
# Собранный фронт на сервере (заливка вручную; в git только .gitkeep)
deploy/prebuilt-web/*
!deploy/prebuilt-web/.gitkeep
deploy/prebuilt-web.zip
backend/node_modules/
backend/dist/
backend/backups/

.idea/

# Nx graph export (генерируется локально, не хранить в git)
docs/frontend/nx-graph.html
docs/frontend/static/

