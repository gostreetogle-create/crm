/**
 * Каталогный товар (Prisma-модель `Product`).
 * Не путать с производственным изделием — см. `backend/README.md` (ManufacturedProduct / таблица `"Product"`).
 */
export type { Product as CatalogProduct } from "@prisma/client";
