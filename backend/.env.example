PORT=3000
CORS_ORIGIN=*

# Минимум 16 символов. В production обязателен; локально можно не задавать (будет dev-заглушка).
# JWT_SECRET=

# Локально (Postgres в Docker или на хосте):
# DATABASE_URL=postgresql://crm:crm@localhost:5432/crm?schema=public
